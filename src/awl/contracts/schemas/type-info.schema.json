{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://w3id.org/awl/contracts/type-info.schema.json",
  "@context": {
    "awl": "https://w3id.org/awl/schema/",
    "@vocab": "https://w3id.org/awl/schema/"
  },
  "x-oold-version": "0.1.0",
  "title": "TypeInfo",
  "description": "What awl.facts reads statically from a LinkedBaseModel subclass. Both link declaration forms are first class; neither is canonical.",
  "type": "object",
  "properties": {
    "identity": {
      "$ref": "https://w3id.org/awl/contracts/identity.schema.json"
    },
    "declared_types": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Instance rdf:types. From x-oold-instance-rdf-type when present, else the type field default. Under allOf the nearest declaration replaces rather than appends."
    },
    "span": {
      "$ref": "https://w3id.org/awl/contracts/source-span.schema.json"
    },
    "fields": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string"
          },
          "is_link": {
            "type": "boolean"
          },
          "is_many": {
            "type": "boolean"
          },
          "target": {
            "type": [
              "string",
              "null"
            ],
            "description": "Target type for a link; null for a literal field."
          },
          "annotation": {
            "type": [
              "string",
              "null"
            ],
            "description": "The annotation as written, e.g. float. Observation, not inference. awl.context derives the xsd:double coercion from it, without which an integer-valued float field round-trips through JSON-LD as xsd:integer. Named annotation and not pythonType because declarations already uses that name for the same thing, and because a language-specific key would contradict the neutral vocabulary."
          },
          "declaration_form": {
            "enum": [
              "Link[T]",
              "LinkedField(link=True)",
              "plain"
            ]
          },
          "arms": {
            "type": "array",
            "items": {
              "enum": [
                "literal",
                "reference",
                "embedded"
              ]
            },
            "description": "The value forms this field admits, per the OO-LD spec."
          }
        },
        "required": [
          "name",
          "is_link",
          "is_many",
          "declaration_form"
        ]
      }
    },
    "schema": {
      "type": "string",
      "format": "uri",
      "description": "The class's own OO-LD schema, when it has a resolvable one. Never invented: a URL guessed from an import path would validate and resolve to nothing."
    }
  },
  "required": [
    "identity",
    "fields"
  ]
}
